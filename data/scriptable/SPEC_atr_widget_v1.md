# Scriptable ATR Widget v1 仕様書（確定版）

> 作成: 2026-06-05 / メインおぱ
> 確定: 2026-06-05（あろさんレビュー反映）
> 実装担当: コー
> 実装ファイル: `data/scriptable/atr_widget.js`
> 関連: `data/scriptable/コー_impl_spec.md`（次タスクで作成）

---

## 1. 設計コンセプト

```
Scriptable = 「価格を見ずに認識するためのライブツール」
  ・チャート画面を開かなくても状態がわかる
  ・数値ラベルだけで「今やる/やらない」が決まる
  ・Wilder ADX依存を捨てる = 精度議論を回避
  ・あろさん感覚を CONFIG 値として固定 = 認識の一貫性
  ・閾値タップで「シンプル編集」可能（コードに触らなくていい）
```

### Widget Web (heatmap_v14) との住み分け

| | Widget Web | Scriptable |
|---|---|---|
| 視点 | マクロ・俯瞰 | ライブ・顕微鏡 |
| 更新 | 1日1回 | 15分（iOS WidgetKit 最短） |
| 時間軸 | 週次〜D1〜H4 | 形成中 H1 / H4 |
| 役割 | 「今週どんな局面か」 | 「今この瞬間どう動いてるか」 |
| データ源 | MT5 → CSV → HTML | Twelve Data API |
| 配置 | ホーム画面 上半分 | ホーム画面 下半分 |

---

## 2. ウィジェットサイズ & レイアウト

**サイズ**: medium (4x2)

**レイアウト**:
```
┌──────────────────────────────────┐
│  XAUUSD Live           07:20:15  │
├──────────────────────────────────┤
│  H1 ATR(16)   2.45    >13.3 🟢   │  ← 閾値タップで設定編集
│  H1 ATR(32)   4.21    >13.3 🟢   │
│  H4 ATR(8)   32.58    >30.0 🔴   │
├──────────────────────────────────┤
│  今週  [+1]   3 / 5   🟡  ⟲      │  ← +1 / リセットボタン
└──────────────────────────────────┘
```

### タップ領域マップ

| 領域 | アクション | URL Scheme |
|---|---|---|
| 閾値表示 ">XX.X" 部分 | 設定ダイアログ起動 | `scriptable:///run/atr_widget?action=settings` |
| [+1] ボタン | カウンタ +1 | `scriptable:///run/atr_widget?action=increment` |
| ⟲ アイコン | カウンタリセット | `scriptable:///run/atr_widget?action=reset` |
| 上記以外（背景） | フル再描画 | `scriptable:///run/atr_widget` |

---

## 3. 表示項目（4項目）

### 3.1 H1 ATR(16)
- 現値（小数2桁）
- 閾値 ">XX.X" 表示（タップで設定編集）
- 状態色

### 3.2 H1 ATR(32)
- 現値（小数2桁）
- 閾値 ">XX.X" 表示（タップで設定編集）
- 状態色

### 3.3 H4 ATR(8)
- 現値（小数2桁）
- 閾値 ">XX.X" 表示（タップで設定編集）
- 状態色

### 3.4 今週トレード回数
- カウント / 上限（例: `3 / 5`）
- [+1] ボタン（タップでカウントアップ）
- ⟲ ボタン（タップでリセット、押し間違い対策で小さく配置）
- 状態色

---

## 4. 状態色

### 4.1 ATR系（HIGH 1本のみ、2段階）
| 状態 | 色 | 意味 |
|---|---|---|
| ATR < HIGH | 🟢 緑 | 適正帯（まだ余裕） |
| ATR ≥ HIGH | 🔴 赤 | 加熱（注意） |

> **凪判定（LOW）は意図的に外した**。
> 理由: レンジで加熱は定義できるが、BU 来たらメンタルブロックになる懸念あり。凪はあろさん側で判断（あろさん明示の方針 2026-06-05）。

### 4.2 トレード回数（3段階）
| 状態 | 色 | 意味 |
|---|---|---|
| count < ideal | 🟢 緑 | 余裕 |
| ideal ≤ count < limit | 🟡 黄 | 適正範囲だが注意ライン突入 |
| count ≥ limit | 🔴 赤 | 超過、要振り返り |

> **設計意図**: `ideal=3` は「ここまでに仕留めたい目標」、`limit=5` は「これ以上は明らかにやりすぎ」。
> 「上限5回やるぞ！」ではなく「3回で済ませるのが理想」のイメージ。

---

## 5. 設定値（CONFIG）

### 5.1 コード冒頭 CONFIG（コードに直書き、初期値兼デフォルト）

```javascript
const CONFIG = {
  // Twelve Data API
  api_key: "<TWELVE_DATA_API_KEY>",
  symbol: "XAU/USD",

  // ATR 加熱境界（HIGH のみ、1本ライン）
  // ※これらは設定ダイアログでも編集可能。編集後は config.json が優先。
  h1_atr16_high:  13.3,
  h1_atr32_high:  13.3,
  h4_atr8_high:   30.0,

  // 今週トレード回数（コード固定、ダイアログ編集対象外）
  weekly_ideal:   3,     // 余裕→注意の境界
  weekly_limit:   5,     // 注意→超過の境界
  reset_weekday:  1,     // 0=日, 1=月... 月曜00:00 にリセット

  // 表示
  refresh_interval_min: 15,
};
```

### 5.2 タップ編集対象（A案：ATR 3値のみ）

| 設定項目 | タップ編集 | コード編集 |
|---|---|---|
| h1_atr16_high | ✅ | ✅ |
| h1_atr32_high | ✅ | ✅ |
| h4_atr8_high | ✅ | ✅ |
| weekly_ideal | ❌ | ✅ |
| weekly_limit | ❌ | ✅ |
| reset_weekday | ❌ | ✅ |
| api_key | ❌ | ✅ |

> ATR 閾値は頻繁に見直す値（月1メンテ）。回数・曜日は固定運用。

### 5.3 メンテ運用
- 月1で「最近のATR水準どうか」を確認 → タップ編集で更新
- **2026-08 集中メンテで再評価予定**

---

## 6. データ取得

### 6.1 エンドポイント
- `GET https://api.twelvedata.com/time_series`
- パラメータ: `symbol=XAU/USD&interval=<1h|4h>&outputsize=<N>&apikey=<KEY>`

### 6.2 必要なリクエスト
| Call | interval | outputsize | 用途 |
|---|---|---|---|
| 1 | `1h` | **64** | H1 ATR(16) と ATR(32) 計算（ATR(32) 初期化のため 64本） |
| 2 | `4h` | 24 | H4 ATR(8) 計算 |

→ **1更新あたり 2 call**
→ 15分更新 × 96回/日 = **192 call/日**（無料枠 800 req/day に余裕）

### 6.3 レスポンス例
```json
{
  "values": [
    { "datetime": "2026-06-05 07:00:00", "open": "4477.00", "high": "4480.50", "low": "4475.10", "close": "4478.20" },
    ...
  ]
}
```
※ `values` は新しい順で返る。ATR計算では古い順に並べ替えてから使う。

---

## 7. ATR 計算（Wilder方式）

### 7.1 True Range
```
TR_i = max(
  high_i - low_i,
  |high_i - close_{i-1}|,
  |low_i - close_{i-1}|
)
```

### 7.2 ATR
- 初期 ATR(N) = TR の単純平均（最初の N 本）
- 更新式（Wilder smoothing）:
  ```
  ATR(N)_i = (ATR(N)_{i-1} × (N - 1) + TR_i) / N
  ```

### 7.3 MT5 iATR との整合性
- Wilder式 + 初期=単純平均 で MT5 iATR と数値一致を目指す
- 丸め誤差・初期化バー数の扱いで微小ズレの可能性あり
- **実用上の許容**: 状態色判定（HIGH 境界の上か下か）が一致すれば OK
- フォワード比較: 実装後 1週間ほど MT5 iATR と Scriptable 値を比較ログ取って許容差確認（あろさん運用）

---

## 8. インタラクション仕様

### 8.1 「+1」ボタン（カウントアップ）
- タップで `?action=increment`
- 起動 → カウンタ +1 → ファイル保存 → 即再描画

### 8.2 「⟲」ボタン（リセット）
- タップで `?action=reset`
- 起動 → カウンタ 0 / last_reset 更新 → 保存 → 再描画
- 確認ダイアログ無し（押し間違い防止はアイコンを小さく配置して物理的に対策）
- iOS の長押し検出はウィジェット内では制限があるため別タップ領域で実装

### 8.3 閾値タップ → 設定ダイアログ
- タップで `?action=settings`
- Scriptable アプリが起動して `Alert` を表示:
  ```
  ┌─────────────────────────┐
  │  ATR 閾値設定            │
  ├─────────────────────────┤
  │  H1 ATR(16) HIGH        │
  │  [   13.3    ]           │
  │                          │
  │  H1 ATR(32) HIGH        │
  │  [   13.3    ]           │
  │                          │
  │  H4 ATR(8) HIGH         │
  │  [   30.0    ]           │
  ├─────────────────────────┤
  │   キャンセル    保存     │
  └─────────────────────────┘
  ```
- 「保存」タップ → `config.json` 書き込み → Script.complete()
- 次回ウィジェット再描画時に新しい閾値が反映

### 8.4 自動リセット判定
- スクリプト起動時、CONFIG.reset_weekday の **直近の該当日** を計算
- `last_reset` < 計算結果 なら、`week_count = 0` にリセット
- 例: 今日 2026-06-09 (火), last_reset 2026-06-02 (月), reset_weekday=1 (月)
  → 直近月曜 = 2026-06-08 → last_reset < 2026-06-08 → リセット実行

---

## 9. 永続化（iCloud Drive）

```
iCloud Drive/Scriptable/
├── atr_widget_state.json    ← トレード回数
└── atr_widget_config.json   ← ATR 閾値（タップ編集対象）
```

### 9.1 atr_widget_state.json
```json
{
  "week_count": 3,
  "last_reset": "2026-06-02"
}
```

### 9.2 atr_widget_config.json
```json
{
  "h1_atr16_high": 13.3,
  "h1_atr32_high": 13.3,
  "h4_atr8_high":  30.0
}
```

### 9.3 読み込み優先順
1. `atr_widget_config.json` が存在すればその値を使う
2. 存在しなければ CONFIG（コード冒頭）の初期値を使う
3. 不正値（NaN、負数）は無視して初期値にフォールバック

### 9.4 iCloud 同期
- iPhone / iPad 間で自動同期
- 同時編集競合は考慮しない（ユーザー1人運用）

---

## 10. エラー処理

| ケース | 挙動 |
|---|---|
| API 応答失敗（4xx/5xx） | 直近の値を表示 + 「API Err」ラベル |
| ネットワークなし | 直近の値 + 「Offline」ラベル |
| 初回起動でデータなし | 「Loading…」表示 |
| iCloud File 読み込み失敗 | 初期値で新規作成 |
| ATR 計算で 0除算 | 該当行を「N/A」表示 |
| 設定ダイアログでの不正入力 | 「無効な値」alert を出して保存中止 |

---

## 11. 更新挙動

- iOS WidgetKit が自動更新（最短 15分）
- 「+1」「⟲」「閾値」タップ時は即座に再描画
- 手動更新: Scriptable アプリから「Run Script」も可能

---

## 12. ファイル構成

```
data/scriptable/
├── SPEC_atr_widget_v1.md       ← この仕様書（メインおぱ）
├── コー_impl_spec.md           ← コー向け実装指示書（次タスクで作成）
└── atr_widget.js               ← コーが実装する Scriptable JS
```

### Mac側保管 vs iPad実機

- Mac側（`data/scriptable/atr_widget.js`） = ソースコード保管・バージョン管理
- iPad の Scriptable アプリにコピーして動作
- 更新時は手動コピー or iCloud Drive 経由

---

## 13. 含まないもの（あろさん明示で外したもの）

- ❌ ATR LOW 閾値（凪判定）— レンジで加熱定義はできるが BU 来たらメンタルブロック懸念
- ❌ 圧縮率（H1 ATR16/32 比、H4 ATR8/46 比）— シンプル優先
- ❌ ADX系（Wilder で MT5 と値が乖離するため別物になる）
- ❌ シグナルアラート Phase 2（ADX依存のため）
- ❌ 凪離脱検知（あろさん側で判断）
- ❌ プッシュ通知

---

## 14. 拡張余地（将来）

現状は最小構成。あろさんが必要と感じたら追加：

| 追加候補 | 理由 |
|---|---|
| H1H4 圧縮率 | ATR短/長 比、加速感の把握 |
| 凪離脱検知 | 直前N本LOW→今HIGHの遷移警告 |
| 中央値ローリング表示 | 「最近の中央値が何か」可視化 |
| プッシュ通知 | 凪離脱 / 上限超過アラート |
| 複数銘柄 | USDJPY/BTCUSD 他観察 |

**設計思想**: 「シンプルなほどデータ品質が上がる」を維持。

---

## 15. 開発ロードマップ

- [x] Phase 1: 仕様書ドラフト作成（2026-06-05）
- [x] Phase 2: あろさんレビュー → 確定（2026-06-05）
- [ ] Phase 3: コー向け実装指示書作成
- [ ] Phase 4: コーが `atr_widget.js` 実装
- [ ] Phase 5: あろさん iPad 実機テスト
- [ ] Phase 6: ATR値の MT5 整合性 1週間フォワード比較
- [ ] Phase 7: 閾値メンテ（2026-08 集中メンテ）

---

## 16. 関連メモリ・ドキュメント

- `[[trading-method-summary]]` — ATR=タイミング, ADX=評価軸
- `[[roadmap-sensory-to-logic-phase]]` — フェーズ2のロジック化
- `[[hardware-mac-environment]]` — Mac/iPad 環境
- `CLAUDE.md` Stage 9 — Scriptableリアルタイムウィジェット構想
