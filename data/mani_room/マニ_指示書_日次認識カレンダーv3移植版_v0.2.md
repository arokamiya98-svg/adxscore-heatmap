# マニ指示書: 日次認識カレンダー v3 移植版（v2ベース + シグナル統合）

> 発行: 2026-06-11 メインおぱ
> 経緯: スクラッチ版 v3 をあろさん実見 → 「v2 マニカレンダーよりだいぶ弱い」。**v2 の強み = 背景色が戦略文脈（H4）×バーが結果（MFE/MAE）の対比構造**だと判明。方針転換: v2 をベースに v3 部品を移植する
> 方式: 一歩ずつ（Step 1 = 最小移植、以後あろさん実見で1個ずつ調整）

---

## 0. 設計哲学の更新（重要）

```
背景色   = 戦略文脈（H4 ADX/DI ベース。現行 v2 のまま）   ← 因果
MFE/MAEバー = 結果（最新の値動き情報）                      ← 結果
シグナル・実弾 = その間で何が起きたか                        ← 今回の追加
```

スクラッチ v3 の失敗: 方向色を H1 にして背景も結果系になり対比が消えた + 数字を削りすぎて認識の足場が消えた。**今回は v2 の見た目を原則一切変えない。**

## 1. 成果物

1. `scripts/generate_daily_calendar_v3.py` — **全面書き直し**（generate_daily_calendar.py をコピーして再出発。スクラッチ版は git 履歴に保存済みなので上書きOK）
2. 出力: `data/trades/processed/daily_calendar_v3.html`（既存パイプライン配線・URL そのまま）
3. レポート: `data/mani_room/マニ_実装レポート_v3移植版_v1.md`

**変更禁止（継続）**: generate_daily_calendar.py / trades_calendar.html / generate_signals_calendar.py / signals_calendar.html

## 2. Step 1 スコープ（これだけ。最小差分主義）

### A1: セルにシグナルドット行を追加
- データ: `mt5_data/signal_fires.csv`（UTF-8-sig）
- 視覚言語は signals_calendar v2 と完全同一: PAT_COLORS 10色 / ▲▼ / pass_all=FALSE は薄表示
- v2 セルの既存レイアウト（SC・↑上↓下数値・H1帯・PAT/ATRバッジ・トレード帯・D1/ATRラベル）を**壊さない位置**に追加（マニ裁量、セル下部の細い行を推奨）

### A2: セルクリック → ドロワー
- signals_calendar v2 / スクラッチ v3 のドロワー実装を流用（シグナルカード + 実トレードカード、MFE/MAE 12/24/36/48h 同一フォーマット、server時間併記、新規理由折りたたみ）
- v2 既存のクリック挙動（円グラフ連動・テーブル等）と衝突しないこと。既存挙動が壊れるくらいなら共存方法はマニ裁量（例: セル本体クリック=ドロワー、既存UI は従来通り）

### A3: SC表記の削除
- セル右上の「SC28」等のスコア数値テキストを削除（あろさん明言「ほとんど見ないからいらない」）
- ホバー title 内のスコア値は残してよい
- **削除はこれだけ**。他のテキスト・数字・金額帯は一切触らない

## 3. Step 2 以降の予告（今回やらない・レポートに記載のみ）

- トレード帯（++59k lot0.03 ★1 形式）を MFE/MAE と同系のバー表示へ（あろさん要望。スケール設計をおぱと詰めてから）
- その他、あろさん実見ごとに1個ずつ

## 4. 完了条件

- [ ] v2 との見た目差分が「シグナルドット行追加 + ドロワー + SC削除」**だけ**であること（レポートに差分一覧を明記）
- [ ] 発火 389 / pass 265 / 抑制 124 全件描画（検証出力）
- [ ] トレード 30件・26日 整合
- [ ] v2 の既存機能（円グラフ4・クリック連動・テーブル・ソート・仮想MFE/MAEバー）が全部生きていること
- [ ] 変更禁止4ファイルの git diff なし
- [ ] 検証コマンド+出力貼付（流儀継続）

---

*v0.2 — 「一歩ずつ、できたのを見て調整」方式。Step 1 は最小移植 + 明言済みSC削除のみ。*
